// console.log(Math.floor(12.5)) // it will return the largest integer part of the number
// console.log(Math.round(10.4)) // it will round the value to the nearest integer
// console.log(Math.ceil(10.4)) // it also downward the value to the nearest integer
console.log(Math.trunc(10.9)) // it will remove the decimal part of the number

const numbers = [1,2,3,4]
numbers[100]= 500
console.log(numbers)

// var a = 5
// var b = 10
// [a, b] = [b, a] // swapping variables using destructuring assignment
// console.log('value of  ',a,b )
